import Node
class Edge:
    def __init__(self, weight):
        self._weight = weight
        self._endNode = Node()

    def getEdge(self):
        #We can update this when we have the Node class.
        return "The edge weight:" +  str(_weight) + "The last Node of this edge: " + _endNode.data
